# Frontier calibration case: vLLM DP placement under pipeline parallelism.
# Fields marked BLOCKED are unresolved decisions. Per the v2 contract a runner
# never fills them from an unstated default; they are answered before the case
# leaves package G2.

case_id: dp_pp_case_001
run_generation: 1
created_at_utc: "2026-09-22"
requesting_user: i-fengyicheng
reviewer_identity: i-fengyicheng
auto_recycle: false

purpose: >-
  Compare the vLLM V1 data-parallel placement chain against Frontier's
  vllm_load_balancing cluster scheduler at pipeline_parallel_size 2, where an
  engine iteration can publish changed request counts without completing any
  batch. Task step 9, plan section 18.6.

selected_checkout:
  frontier_worktree: /data/ycfeng/Frontier/.worktrees/issue26-correctness-pr
  frontier_branch: fix/issue26-correctness-pr
  frontier_commit: BLOCKED  # pinned at simulator-run, after package P2

groundtruth_checkout_path: /data/ycfeng/Frontier/.real-engine/vLLM-BS
groundtruth_branch: feature/frontier-comparison-instrumentation
groundtruth_ref: refs/heads/feature/frontier-comparison-instrumentation
groundtruth_commit: 63ac6c6b9a6bdf5098fce12e349ff8ffad1af3bf
groundtruth_tree_dirty: false
groundtruth_remote_url: https://github.com/fwyc0573/vLLM-BS.git
groundtruth_remote_tip: ea95f571e20937c7c908c6d59ddd1cd6bf9268f1
groundtruth_diff_artifact: g1_instrumentation.diff
groundtruth_diff_sha256: b6400d29710c6f0fac4d0e26c9078177a741d3984f9a07a72f838838eafcf8c7
groundtruth_dirty_patch_sha256: null
groundtruth_overlay_patch_applied: false
groundtruth_commit_amendment:
  amended_at_utc: "2026-09-23"
  superseded_commit: 494b9f327036d4493034a9b37ebb343354884e01
  superseded_diff_sha256: 84fc24db0e2411268a93f8be7ca5f8e4e5072ea09d86063cac2cfb98381feb2c
  runs_with_superseded_commit: 0
  reason: >-
    case_init.md records 494b9f327, whose writer buffered records and flushed
    them at interpreter exit. vLLM 0.10.2 forks its engine cores and DP
    coordinator by default and stops them with SIGTERM, so neither path runs
    the exit flush and those records would be lost. 63ac6c6b9 writes every
    record through as it is made. case_init.md is immutable and is not edited;
    this block is the amendment.
  evidence: ../g1_writer_probes/
groundtruth_weight_mode: dummy
real_weight_download: false

groundtruth_instrumentation:
  gate_env_var: VLLM_FRONTIER_DP_PLACEMENT_LOG_DIR
  output: one JSONL file per process, named dp_placement_<pid>.jsonl
  buffering: write-through; one line-buffered file per process, one JSON line per record, seq restarted in a forked child
  changed_files:
    - vllm/v1/frontier_trace.py
    - vllm/v1/engine/core.py
    - vllm/v1/engine/coordinator.py
    - vllm/v1/engine/core_client.py
  record_kinds:
    engine_iteration:
      written_by: DPEngineCoreProc.run_busy_loop
      join_key: [engine, wave, step]
      fields: [branch, applied_output, queue_occupancy, scheduled_new_req_ids,
               num_scheduled_tokens, waiting, running, published]
    coordinator_receive:
      written_by: DPCoordinatorProc.process_input_socket
      join_key: [engine, wave, step]
      fields: [waiting, running, disposition, latched_counts]
    coordinator_publish:
      written_by: DPCoordinatorProc.process_input_socket
      join_key: [snapshot]
      fields: [counts, counts_source, wave, engines_running]
    frontend_snapshot:
      written_by: DPLBAsyncMPClient stats update task
      join_key: [snapshot]
      fields: [counts, wave, engines_running]
    frontend_route:
      written_by: DPLBAsyncMPClient.get_core_engine_for_request
      join_key: [request_id, snapshot]
      fields: [engine, counts, score, start_index, reservation]
  wire_change: >-
    The coordinator's front-end publication carries a fourth element, the
    snapshot id. Both the only sender and the only receiver are in this
    checkout. Without it an applied snapshot cannot be tied to the engine
    reports it came from, which is the join T1 needs.
  scheduler_py_unchanged: >-
    The plan listed vllm/v1/core/sched/scheduler.py as a likely change for a
    dp_rank column. It is not needed: SchedulerOutput already carries
    scheduled_new_reqs and total_num_scheduled_tokens, and the engine record is
    written by the engine that owns the rank.

topology:
  vllm: "--data-parallel-size 2 --pipeline-parallel-size 2 --tensor-parallel-size 1 --enable-expert-parallel"
  frontier:
    attn_dp: 2
    attn_tp: 1
    moe_tp: 1
    moe_ep: 2
    num_pipeline_stages: 2
    num_replicas: 1
    cluster_scheduler: vllm_load_balancing
    replica_scheduler: vllm_v1
  devices: 4x H800
  blocked_by: null
  unblocked: >-
    W9-01 is resolved: PR 36 merged into main, merged forward here as dd9b8d9,
    composition check passed on 03d5f24 (issues.md W9-01, Resolution).

model:
  name: Qwen3MoeForCausalLM
  config: data/config/models/Qwen3-30B-A3B-tiny.json
  layers: 8
  experts: 16
  top_k: 8
  load_format: dummy
  skip_tokenizer_init: true

workload:
  source: single Frontier trace CSV (arrived_at, num_prefill_tokens, num_decode_tokens)
  request_id_namespace: BLOCKED   # set by G2 make_trace.py
  request_id_encoding: "x-request-id header, propagated to the engine request id"
  formal_request_ids: BLOCKED
  warmup_request_ids: BLOCKED
  warmups: 3

modes:
  groundtruth_clean:
    status: NOT_RUN
    artifact_path: /mnt/codesign-exp/ycfeng/frontier/dp_pp_calibration/
  simulator:
    status: NOT_RUN
  simulator_control_completion_reporting:
    status: NOT_RUN

decisions:
  - decision_id: D-gpu-authorization
    question: >-
      Does the S0 2-GPU smoke and the S1 4-GPU ground-truth run have the user's
      GPU authorization on charged_group codesign?
    answer: "授权上述1-2，推进W9-05"
    outcome: >-
      Item 2 of the numbered next actions it answers was the Step 9 vLLM-side
      GPU comparison G3-G5. Authorized within the plan's budget: at most 3 jobs
      of at most 1 h each, H800, charged_group codesign only (steptron_ci is
      paused by the user), submitted through StepMind RJobBackend.
    decided_at_utc: "2026-09-23"
  - decision_id: W9-01-scope
    question: >-
      Is the pre-existing stage admission deadlock fixed inside this feature
      branch, fixed as a separate correctness item, or does step 9 pause?
    answer: "采纳你的推荐，继续"
    outcome: >-
      Fixed as a separate correctness item on fix/stage-admission-ordering,
      draft PR 36 (rule dac4e69). The Frontier side of this case stays blocked
      until PR 36 merges into main, is merged forward, and passes the G3b
      composition check (issues.md W9-01, Resolution).
    decided_at_utc: "2026-09-22"
    resolved: >-
      2026-09-23. PR 36 merged into main as 4ab1964, merged forward as dd9b8d9,
      composition check passed on 03d5f24.

status: INITIALIZED
