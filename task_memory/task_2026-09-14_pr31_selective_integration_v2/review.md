## Modification History

| Date       | Summary of Changes |
| ---------- | ------------------ |
| 2026-09-15 | Added the independent completion-audit checkpoint; corrected scoped CPU-closure wording and classified all R01–R14 against the v1.1 acceptance criteria. |
| 2026-09-15 | Added exact-HEAD R11/R12, broad CPU, and hardware-boundary review checkpoint; D02/D03 remain pending. |
| 2026-09-15 | Final verification rerun passed 220 focused tests with clean compile/diff/status evidence. |
| 2026-09-15 | Added and verified a direct actual-replica-ID routing contract test in commit `7b6a3eb1`. |
| 2026-09-15 | Rechecked the complete CPU unit suite after routing repair: 3276 passed with the same 19 baseline failures and no candidate-only failure. |
| 2026-09-15 | Re-reviewed monolithic MoE routing identity and manager device-event path derivation; focused CPU acceptance passed 219/219. |
| 2026-09-15 | Reviewed the persisted real CPU hybrid Simulator E2E test and recorded its control-flow PASS plus model-manager seam limitation. |
| 2026-09-15 | Re-reviewed current exact HEAD; confirmed the SGLang graph replay tuple-contract blocker is already fixed and closed. |
| 2026-09-14 | Completed Review 2 across hybrid timing, runtime-family resolution, profiling identity, replay orchestration, and final CPU regression. |
| 2026-09-14 | Added the Review 1 audit template required by plan v2. |
| 2026-09-14 | Completed the Increment 4 semantic seam review for hybrid GDN/full-attention binding. |

# Review 1 — Attention Assumption Inventory

Status: Review 2 completed; scoped selective-integration audit passed, with baseline failures and hardware skips explicitly recorded.

| Area | Search targets | Classification | Evidence / action |
| ---- | -------------- | -------------- | ----------------- |
| Family binding | `frontier/attention/model_binding.py`, `families.py`, `profiling_mapping.py` | `HOMOGENEOUS_ONLY` | `bind_attention_family(config)` returns one whole-model `AttentionFamilyBinding`; the registry currently contains dense-KV, latent-MLA, and frozen DSA families. No layer-id argument or hybrid resolver exists. Increment 4 must add an explicit per-layer binding and make the whole-model binder reject hybrids. |
| MLA/KV helpers | `frontier/attention/memory.py`, `frontier/config/model_config.py`, `frontier/profiling/common/model_config.py`, `frontier/kv_cache_transfer/` | `HOMOGENEOUS_ONLY` / `UNSUPPORTED_HYBRID_PATH` | `get_attention_runtime_kv_layout()` consumes one family and rejects `kv_factor=None`; the model and profiling configs call the whole-model binder for runtime KV/head helpers. `AnalyticalKVCacheTransferPredictor` calculates one global layout and must reject new GDN P→D at the feature entry. |
| Operator selection | `frontier/operators/`, `frontier/profiling/*/profiling_plan.py` | `HOMOGENEOUS_ONLY` | Operator families and profiling plans are selected by model-wide `is_moe`/architecture metadata. `AttentionProfilingFeatureSchema` uses one family per dataframe. Increment 4/8 must register GDN operators and keep mixed-family rows explicit; no same-batch mixed GDN path is present. |
| Training/model manager | `frontier/execution_time_predictor/`, `frontier/training/` | `HOMOGENEOUS_ONLY` | The shared manager currently trains attention/FFN families from one model profile and exposes `eager`/`kernel_only` registries. The Increment 3 plumbing now adds an isolated `device_event` registry/path identity; per-layer GDN training and predictor dispatch remain pending for Increment 8. |
| Simulation consumers | `frontier/entities/execution_time.py`, `frontier/events/`, `frontier/scheduler/`, `frontier/metrics/` | `HOMOGENEOUS_ONLY` | `ExecutionTime` stores one component set plus `num_layers_per_pipeline_stage`; scheduler/event consumers consume the aggregate object. Increment 5 must migrate these consumers to ordered real-layer results and reject unsupported hybrid lifecycle features before dispatch. |
| Parameter/memory | `frontier/utils/param_counter.py`, `frontier/scheduler/utils/memory_planner.py` | `HOMOGENEOUS_ONLY` | Parameter counting and capacity planning use one model-wide attention shape and KV assumptions. Increment 6 must add schedule-aware GDN state/weight accounting while preserving existing Qwen3-Next homogeneous totals. |
| Indirect naming | `frontier/model_architectures.py`, string accessors, error messages, tests | `PER_LAYER` for existing typed contract metadata; `HOMOGENEOUS_ONLY` for attention binding | Existing internal `linear_attention` identifiers were removed in Increment 1, while upstream Qwen layer-type strings remain legal. Typed model architecture contracts already carry layer-kind metadata for FFN selection, but no attention family per layer is resolved yet. |

## Review 1 conclusion

The current main branch assumes one attention family and one timing domain per model/stage. The safe extraction seam is the existing typed family/operator registry plus the model-config resolver; the scheduler and memory consumers must wait for the explicit per-layer contract. No PR-only whole-model GDN predictor, SGLang correlation stack, or runtime-cost subsystem is admitted by this review. The inventory is complete for the plan's listed call sites; unresolved work is recorded as pending Increment 4–6 implementation rather than inferred from keyword matches.

Allowed classifications are `PER_LAYER`, `HOMOGENEOUS_ONLY`, or `UNSUPPORTED_HYBRID_PATH`. A keyword hit is only an investigation lead.

## Increment 4 seam review

| Target Component/Phase | Reviewer Agent Identity | Inspected Artifacts | Identified Issues/Anomalies | Remediation/Verification Code Actions Taken |
| ---------------------- | ----------------------- | ------------------- | --------------------------- | --------------------------------------------- |
| Hybrid attention resolver and GDN family | `/root` | `frontier/attention/gdn/config.py`, `frontier/attention/families.py`, `frontier/attention/model_binding.py`, `frontier/config/model_config.py`, `frontier/profiling/common/model_config.py` | Whole-model binding is unsafe for a hybrid schedule; upstream Qwen3-Next `linear_*` metadata must not activate GDN; GDN must not enter KV-helper paths. | Added immutable `LayerAttentionSpec`, explicit `bind_layer_attention(config, global_layer_id)`, Qwen3.5-only activation, `FIXED_STATE` family, and fail-fast legacy binder/runtime KV helpers. Focused semantic tests pass. |
| GDN profiling schema and fixture | `/root` | `frontier/attention/profiling_mapping.py`, `frontier/profiling/gdn/inputs.py`, `tests/fixtures/pr31_hybrid/README.md`, `tests/fixtures/pr31_hybrid/gdn.csv` | Initial synthetic CSV exposed only median columns and could not satisfy the shared validator. | Added CPU-safe `GDNProfileInput`, stable raw feature schema, explicit test-only fixture documentation, and complete min/max/mean/median/std/count columns. `DEVICE_EVENT` validator passes. |
| Qwen3.8 model metadata and quantization | `/root` | `data/config/models/Qwen3.8-2.4T-A95B-Quark-MXFP4.json`, `frontier/config/quantization_manager.py`, `frontier/model_architectures.py` | Quark config must select FP4 only for MoE grouped GEMM while preserving BF16 defaults elsewhere. | Added selective operation metadata and experimental profile limitations. Structural load pins 92 layers, 69 GDN, 23 full-attention, and only `moe_grouped_gemm` as FP4. |

Increment 4 review result: **PASS** for the scoped semantic/configuration seam. Runtime scheduler, memory planner, trainer/model-manager, and GPU producer integration remain intentionally pending in later increments.

## Increment 5 review — timing ownership and compatibility

| Target Component/Phase | Reviewer Agent Identity | Inspected Artifacts | Identified Issues/Anomalies | Remediation/Verification Code Actions Taken |
| --- | --- | --- | --- | --- |
| Single-layer timing contract | `/root` | `frontier/entities/execution_time.py`, predictor stage paths, layer-scaling tests | Historical direct callers still construct aggregate-compatible `ExecutionTime`; identity-bearing results must remain one real layer | Added explicit layer identity, fail-fast partial identity validation, `as_single_layer()` deep-copy conversion, and compatibility-only identity-free construction. Focused execution-time tests passed. |
| Ordered stage aggregation | `/root` | `frontier/entities/stage_execution_time.py`, dense/MoE/disaggregation predictors, stage tests | Stage-level communication and terminal work must not be duplicated per layer | Added explicit stage owner and ordered layer records, aggregate model/diagnostic/op views, and first-layer compatibility probes. Predictor and regression suites passed. |
| Metrics adapter compatibility | `/root` | stage-to-legacy metrics adapter and MoE fidelity artifacts | First implementation summed private per-layer fields, causing a 32x attention all-reduce metric in the legacy payload | Changed private compatibility access to first-layer raw values; explicit stage aggregates remain available. Five-case fidelity rerun passed with zero failures. |
| Attention query cache | `/root` | predictor cache key/build/hit paths and query-cache tests | Cache must exclude only layer ID and mutable batch ID while retaining family, phase, shape, stack, model, and routing-relevant identity | Added immutable numeric `AttentionTime` cache values, deep-copy hits, and complete identity key. Focused cache and MoE semantic tests passed. |

Increment 5 review result: **PASS** for the scoped per-layer timing migration. The measured CPU wall-time increase is recorded as an observation; no unexplained numerical divergence remains in the five selected fidelity cases. Increment 6 must extend the resolved layer contract into parameter/state memory and initialization guards.

## Increment 6 review — state/memory boundaries and fail-fast runtime support

| Target Component/Phase | Reviewer Agent Identity | Inspected Artifacts | Identified Issues/Anomalies | Remediation/Verification Code Actions Taken |
| --- | --- | --- | --- | --- |
| Fixed GDN state memory | `/root` | `frontier/attention/gdn/memory.py`, `frontier/attention/gdn/config.py`, `tests/unit/test_gdn_increment6_memory.py` | State must remain fixed per request/layer/worker; speculative-token sizing was outside the approved initial API | Added TP-aware convolution/recurrent shapes and byte properties; removed speculative-token sizing from the public layout API. Focused state tests pass. |
| Schedule-aware parameter accounting | `/root` | `frontier/utils/param_counter.py`, Qwen3.8 model metadata, `tests/unit/test_gdn_increment6_memory.py` | A hybrid PP stage cannot use global representative counts; FP32 `A_log` and the D57 approximation need separate treatment | Added actual stage layer composition, GDN sharding, FP32 `A_log`, resident-stage attention bytes, and a direct D57 MXFP4 approximation comment. Synthetic tests and a real Qwen3.8 TP1/PP4 structural/capacity check pass. |
| KV/state capacity planning | `/root` | `frontier/scheduler/utils/memory_planner.py` | Combining the largest GDN count and largest full-attention count from different stages would over/under-reserve memory | Select one resident stage by its total attention/GDN bytes, reserve fixed state for `max_num_seqs`, and use only that stage's full-attention count for KV pages. Existing non-GDN memory tests pass. |
| Simulator-only state lifecycle | `/root` | `frontier/attention/gdn/state.py`, `tests/unit/test_gdn_state_lifecycle.py` | Waiting requests retain state; state recovery cannot be represented by tensor snapshots in the simulator | Added request/slot ownership tokens with allocate/retain/resume/release, exhaustion handling, deterministic reuse, and no tensor fields. Lifecycle tests pass. |
| Runtime rejection boundaries | `/root` | `frontier/attention/gdn/guards.py`, `frontier/config/config.py`, analytical transfer predictor, vLLM scheduler preemption path, `tests/unit/test_gdn_runtime_guards.py` | Unsupported prefix cache, P→D, speculative/MTP, PP/EP/attention-DP/cross-node, and state-dropping preemption must fail before mutation | Added one guard function and wired it into config initialization, transfer-size entry, and preemption before counters/queues change. Waiting retention remains allowed. Guard and preemption tests pass. |
| Compatibility regressions | `/root` | full `tests/unit` run and admission/registry tests | Lightweight mock schedulers lacked `_replica_config`; legacy sentinel helpers were not `ExecutionTime`; GDN registry expectation lagged the new family | Added optional-config lookup, typed pass-through at predictor test-double boundaries, and GDN registry expectations. Focused compatibility suite passes; full unit returns to the 19 baseline failures. |

Increment 6 review result: **PASS** for the scoped memory/lifecycle/guard implementation. The full CPU unit regression has no new failure relative to baseline. AMD/MI355X runtime evidence remains unavailable and is explicitly skipped.

## Increment 12 review — shared routing semantics

| Target Component/Phase | Reviewer Agent Identity | Inspected Artifacts | Identified Issues/Anomalies | Remediation/Verification Code Actions Taken |
| --- | --- | --- | --- | --- |
| Routing ratio ownership | `/root` | `frontier/moe_ep_workload.py`, standard and disaggregation predictor allocation methods | The same distribution formulas were duplicated in two predictors, creating drift risk; integer token allocation belongs downstream. | Added one pure helper and kept `materialize_layer_ep_workload()` responsible for integerization and EP ownership. Exact 48-case old-formula comparison passed. |
| Predictor equivalence | `/root` | `SklearnMoEExecutionTimePredictor._init_global_routing_allocations()`, `SklearnDisaggregationExecutionTimePredictor._generate_expert_allocations()` | Disaggregation has legacy method parameters and final renormalization; removing those contracts could alter callers or floating-point output. | Kept method signatures and final renormalization, ignored only unused ownership parameters, and verified standard/disaggregation ratio equivalence. |
| Replay collection boundary | `/root` | `tests/unit/test_moe_ep_baseline_replay.py`, detached `0d1b87a4` run | Collection fails in a pre-existing `frontier.operators.families` import cycle. | Reproduced on baseline and candidate; recorded as baseline issue without adding unrelated fallback logic. |

Increment 12 review result: **PASS** for the scoped CPU routing-helper refactor. No model expert-count/top-k source was duplicated, no deferred routing trace path was enabled, and no live SGLang router was observed.

## Increment 8 review — standard GDN training and prediction

| Target Component/Phase | Reviewer Agent Identity | Inspected Artifacts | Identified Issues/Anomalies | Remediation/Verification Code Actions Taken |
| --- | --- | --- | --- | --- |
| Shared GDN feature contract | `/root` | `frontier/attention/gdn/features.py`, GDN fixture, Increment 8 unit tests | Decode history/context length must not become a hidden cost feature; mixed-phase batches have no single estimator. | Added one `GDNBatchFeatures` builder for rows and runtime batches, explicit physical features, statefulness derivation, and pure-phase rejection. Focused tests pass. |
| GDN trainer and artifact identity | `/root` | `frontier/training/gdn_trainer.py`, `frontier/training/base_trainer.py`, fixture CSV | Six operator/phase tasks must remain measurement/runtime-stack homogeneous; `gdn_layer_e2e` must not contaminate standard targets. | Added scoped identity filtering, runtime-contract uniqueness checks, deterministic single-row fitting, estimator metadata, exact lookup, and `gdn_manifest.json`; fixture CLI generated all six artifacts. |
| Artifact-backed predictor | `/root` | `frontier/execution_time_predictor/gdn_predictor.py`, `time_components.py`, predictor tests | Runtime fitting, cross-TP scaling, clipping, phase fallback, or mixed measurement families would invalidate the contract. | Predictor only loads manifest/artifacts, validates identity, looks up exact rows first, warns on extrapolation, and returns structured `AttentionOperatorTimes`. Identity, phase, and out-of-range tests pass. |
| Training CLI/config/docs | `/root` | `frontier/training/cli.py`, `frontier/config/config.py`, `docs/training/README.md` | Public dispatch must expose `gdn.csv` without adding an unsupported kernel-only GDN path. | Added direct `gdn` parser/dispatch, `gdn_input_file`, and documented `DEVICE_EVENT`, manifest, and experimental-artifact boundaries. CLI help and compile checks pass. |
| Simulator manager seam | `/root` | `frontier/execution_time_predictor/shared_prediction_model_manager.py`, `sklearn_execution_time_predictor.py` | Hybrid models still need full-attention estimator integration before complete E2E. | Added GDN artifact loading and GDN-layer predictor dispatch; intentionally skip homogeneous attention training for hybrid models pending Increment 14A/B. Fresh manager load test passes; full hybrid E2E remains open. |

Increment 8 review result: **PASS** for the scoped CPU training/prediction/artifact seam. **OPEN:** Increment 14A/B must connect full-attention artifacts and prove ordered per-layer hybrid request/metrics completion. `SKIP: AMD/MI355X hardware unavailable` remains in force.

## Increment 14A/B review — early CPU hybrid dispatch checkpoint

| Target Component/Phase | Reviewer Agent Identity | Inspected Artifacts | Identified Issues/Anomalies | Remediation/Verification Code Actions Taken |
| --- | --- | --- | --- | --- |
| Full-attention layer dispatch | `/root` | `frontier/execution_time_predictor/sklearn_execution_time_predictor.py`, `frontier/attention/model_binding.py`, `tests/unit/test_gdn_hybrid_e2e_increment14ab.py` | The GDN branch was artifact-backed, but a full-attention layer would call the homogeneous binder and fail on the hybrid config. | Added explicit per-layer binding after the GDN branch; the synthetic 8-layer case reaches both GDN and dense attention layers and keeps family identity. |
| MoE stage identity | `/root` | `frontier/execution_time_predictor/sklearn_moe_execution_time_predictor.py`, `frontier/entities/execution_time.py`, `frontier/entities/stage_execution_time.py` | `ExecutionTime` objects created by the MoE internal path did not carry resolved layer identity; compatibility expansion could replace an existing family identity with dense defaults. | Attach `global_layer_id`/family/variant from `bind_layer_attention()` and preserve source identity during `StageExecutionTime.from_execution_time()` expansion. |
| Model-manager training seam | `/root` | `frontier/execution_time_predictor/shared_prediction_model_manager.py`, manager load test, hybrid fixture | A temporary `not has_gdn` condition skipped full-attention training for hybrid models; MLA detection also called the unsafe whole-model binder. | Train attention models for hybrid clusters and force hybrid configurations out of `_is_mla_family()`. Fresh GDN artifact load passes with strict `DEVICE_EVENT` identity. |
| CPU evidence boundary | `/root` | `tests/unit/test_gdn_hybrid_e2e_increment14ab.py`, `/data/ycfeng/tmp/pr31-inc14ab-fidelity-20260914-run2` | No AMD/MI355X host is available; unrelated FFN/communication values are deterministic test hooks rather than profiled data. | Recorded the limitation explicitly. The checkpoint proves control flow, layer identity, phase semantics, and stage aggregation only. `SKIP: AMD/MI355X hardware unavailable`. |

Increment 14A/B early checkpoint result: **PASS for the scoped CPU dispatch seam; OPEN for production-data full simulator request/metrics E2E.** Focused hybrid/manager/stage/predictor tests passed 76/76, and the selected existing dense fidelity case passed 1/1. No AMD profiling or latency/groundtruth parity is claimed.

## Increment 14A/B review — persisted real CPU Simulator E2E

| Target Component/Phase | Reviewer Agent Identity | Inspected Artifacts | Identified Issues/Anomalies | Remediation/Verification Code Actions Taken |
| --- | --- | --- | --- | --- |
| Real Simulator control flow | `/root`, `/root/review_arch` | `tests/unit/test_gdn_hybrid_e2e_increment14ab.py::test_hybrid_gdn_real_simulator_cpu_e2e`, pytest output, generated request/system/ledger/trace artifacts | The temporary probe had incorrectly queried GDN attention on `include_attention=False` post-attention FFN lanes, producing `query_len must be positive`; the first persistent test also needed complete zero-valued GDN structured operator keys for trace mapping. | Persisted the probe with zero-attention handling and complete GDN operator schema. Real `Simulator`, `ReplicaConfig`, MemoryPlanner, scheduler, EP events/barriers, continuation, metrics writer, and op-level traces now execute successfully. Focused command passes **4/4**; one request completes with 18 tokens, two stage-ledger rows, and GDN/dense layer identities `{0,1,2,4,5,6}` / `{3,7}`. |
| Fresh model-manager boundary | `/root/review_arch` | `_train_gdn`, `_write_production_profile_fixture`, `test_hybrid_gdn_production_constructor_cpu_e2e`, `test_fresh_model_manager_loads_hybrid_gdn_artifact`, simulator test seam | The persistent request/metrics test still injects a prepared predictor for unrelated operator timings, while a synthetic standard-profile fixture is sufficient to exercise the real manager constructor. | Added a production-constructor CPU E2E with real manager, GDN artifact, standard attention/MoE model training, Registry construction, Simulator creation, and request completion. Kept the prepared-predictor seam explicit for the separate full-flow test because model-specific production CSVs are unavailable. |

Increment 14A/B persistent CPU E2E result: **PASS for control flow, GDN artifact dispatch, per-layer identity, stage-ledger accounting, request completion, and metrics output.** The synthetic production-profile constructor E2E also passes through real manager/predictor training and loading. **OPEN:** production-data fidelity with complete model-specific standard attention/MoE CSVs remains unavailable. `SKIP: AMD/MI355X hardware unavailable` remains in force for ROCm runtime, timing, benchmark, and groundtruth validation.

## Increment 14A/B — monolithic routing identity closure

| Target Component/Phase | Reviewer Agent Identity | Inspected Artifacts | Identified Issues/Anomalies | Remediation/Verification Code Actions Taken |
| ---------------------- | ----------------------- | ------------------- | --------------------------- | --------------------------------------------- |
| Replica identity in shared MoE routing | `/root` | `frontier/entities/base_entity.py`, `frontier/entities/replica.py`, `frontier/simulator.py`, `frontier/execution_time_predictor/random_forrest_execution_time_predictor.py`, `frontier/execution_time_predictor/sklearn_moe_execution_time_predictor.py`, `frontier/moe_ep_workload.py` | `Replica.id` is process-global, but monolithic shared routing details were keyed by local `range(replica_count)`. Focused-suite ordering exposed `target_replica_id=1` against a map containing only `0`. | `Simulator` passes `Cluster.replicas.keys()` into the Registry; the Random Forest wrapper forwards IDs for MoE predictors; `_build_shared_routing_details()` validates and uses exact IDs, preserving local keys for direct tests without supplied IDs. Production constructor and routing identity checks pass. |
| Training path identity | `/root` | `frontier/execution_time_predictor/shared_prediction_model_manager.py`, `tests/unit/test_measurement_family_selector.py` | The manager's complete path API gained device-event derivatives, but the expected contract had not yet listed them. | Added explicit configured-or-derived device-event paths for compute/attention/MoE and updated the exact dictionary contract. Empty fallback paths remain empty. |

Closure evidence: the original complete focused command passed **219 tests**, and the final command including the direct actual-ID routing contract passed **220 tests in 15.05s**. The production constructor passed **1 test in 4.60s** on the follow-up rerun, and the manager path contract passed **1 test**. Compileall and diff check passed; no AMD/ROCm runtime, benchmark, or groundtruth claim is made.

## Review 2 — final selective-integration audit

| Target Component/Phase | Reviewer Agent Identity | Inspected Artifacts | Identified Issues/Anomalies | Remediation/Verification Code Actions Taken |
| --- | --- | --- | --- | --- |
| GDN structured operator schema | `/root` | `frontier/execution_time_predictor/gdn_predictor.py`, `frontier/metrics/op_trace_utils.py`, `tests/unit/test_gdn_hybrid_e2e_increment14ab.py`, persistent simulator trace output | The predictor returned only the active phase core key; op-level structured trace consumers require both prefill and decode core keys, causing a decode-path schema error during the real Simulator E2E. | Added explicit zero-valued inactive-phase keys in `predict_attention_time()`. Re-ran the focused hybrid suite (**4 passed**), compileall, and diff checks. |
| --- | --- | --- | --- | --- |
| Hybrid runtime-family resolution | `/root` | `frontier/attention/model_binding.py`, `frontier/profiling/common/model_config.py`, `tests/unit/test_hybrid_runtime_family.py`, `tests/unit/test_mla_model_config_contracts.py` | The new runtime resolver correctly selects the unique full-attention family for hybrid cache/layout metadata, but replacing the profiling module's imported binder removed an established monkeypatch seam and broke the homogeneous MLA contract test. | Restored the module-level `bind_attention_family` import and routed only configs with GDN layers through `resolve_runtime_attention_family()`. The hybrid/runtime and MLA/model-config lane passed 15/15; no whole-model hybrid binder fallback is permitted. |
| StageExecutionTime metrics and tracing | `/root` | `frontier/entities/stage_execution_time.py`, `frontier/metrics/metrics_store.py`, `frontier/metrics/op_trace_utils.py`, `tests/unit/test_metrics_stage_execution_time.py` | Treating a stage's per-layer operation map as homogeneous `ExecutionTime` duplicated layer rows and ledger charges, and classified GDN core timing as dense attention. | Added dedicated stage-layer trace/operation emission, GDN family-specific metadata, one-time stage-owned pipeline/CPU/terminal accounting, and a stage-aware ledger branch. Review 2 focused tests passed; existing metrics/MLA/MoE/FFN coverage passed 91/91. |
| SGLang replay orchestration | `/root`, `/root/review_code` | `frontier/profiling/experimental/sglang/graph_replay.py`, `frontier/profiling/experimental/sglang/routed_moe_replay.py`, `tests/unit/test_sglang_graph_replay_orchestration.py`, `tests/unit/test_sglang_experimental_increment13.py` | An earlier review raised a tuple-contract concern because native builders have different raw return lengths. The current exact HEAD had to be checked to ensure this was not still a runtime blocker. | `_make_replay_call()` normalizes Dense/GDN/Attention/MoE/legacy builders to `(fn, reset, check, backend, attention_spec, attention_workload, moe_spec)`, while preserving mutable-state reset and reference checks. Current exact HEAD re-review: `test_sglang_experimental_increment13.py` **7 passed**, compileall and diff check passed. Routed replay still reuses Frontier routing semantics and all outputs remain experimental. |
| GDN artifact identity | `/root` | `frontier/training/gdn_trainer.py`, `frontier/execution_time_predictor/gdn_predictor.py`, `frontier/execution_time_predictor/shared_prediction_model_manager.py`, GDN identity tests | A trained artifact could otherwise remain loadable after its source CSV changed, making predictions stale while retaining the same nominal model identity. | Added the GDN CSV SHA-256 to the manifest identity and revalidated it in `GDNPredictor.from_directory(..., dataset_path=...)`; the model manager passes `gdn_file`. Changed-data stale-artifact tests pass. |
| Final broad regression and documentation | `/root` | Full `tests/unit` logs at `/data/ycfeng/tmp/pr31-final-unit-578785bb.log` and `/data/ycfeng/tmp/pr31-full-unit-20260915-routingfix.log`, focused reports, `docs/cli/README.md`, `docs/profiling/README.md`, `docs/profiling/ROCM_MI355X.md`, `docs/training/README.md`, `examples/profiling/README.md` | Full candidate run must be compared to baseline without hiding unrelated repository failures; top-level `README.md` contracts remain stale but are outside the requested documentation scope. | Post-fix full unit result is 3276 passed, 19 failed, 25 skipped, 576 warnings, with the same 19 failure names as baseline (the count increase reflects the newly covered focused tests). Focused lanes and compileall pass. Added the examples profiling modification history while leaving top-level `README.md` unchanged. |

### Review 2 result

Review 2 is **PASS for the scoped selective integration and CPU acceptance path**. The focused-suite routing identity regression and manager path-contract drift were fixed and reverified. The post-fix full-unit run has the same 19 baseline failures and no candidate-only failure. The remaining failures are repository conditions: missing config/debug/analysis assets and stale top-level release documentation contracts. Production hybrid profiling, ROCm device-event execution, SGLang/AITER/HIP graph runtime, RCCL runtime, AMD benchmark comparison, and groundtruth parity remain **SKIP: AMD/MI355X hardware unavailable**.

The initial SGLang graph-replay tuple concern is **closed** by the current exact-HEAD re-review. The implementation already normalizes every native builder at the `make_primitive()` boundary, and the CPU orchestration regression covers the differing raw builder tuple shapes.

## v1.1 exact-HEAD closure checkpoint — 2026-09-15

| Target Component/Phase | Reviewer Agent Identity | Inspected Artifacts | Identified Issues/Anomalies | Remediation/Verification Code Actions Taken |
| --- | --- | --- | --- | --- |
| R11 clean fidelity | `/root` | `/data/ycfeng/tmp/pr33-r11-fidelity-20260915-b8cecf53/manifest.json`, `results.json` | Final evidence must bind to the post-cache candidate rather than the earlier `8c976736` run. | Re-ran all 58 clean baseline/candidate cases with `--workers 8`: **58 passed, 0 failed**; no stable beyond-tolerance or discrete difference triggered D01. |
| R12 wall-clock blocker | `/root` | `/data/ycfeng/tmp/pr33-r12-paired-20260915-final-b8cecf53/`, cProfile artifacts, `frontier/entities/stage_execution_time.py` | Historical 9.42x regression was rejected by the review plan; dense residual remains after causal optimization. | Three interleaved unprofiled repetitions per case completed 18/18. Repeated stage aggregation was cached in `b8cecf53`; MoE is 1.011424x, while dense cases remain 1.504303x and 1.407252x in `sim_wallclock_s`. Residual is open under D02 and has no implied budget. |
| CPU broad regression | `/root` | Persistent full-unit log and eight concurrent broad-group logs under `/data/ycfeng/tmp` | Broad CPU coverage must execute after the post-cache source change and preserve baseline failure boundaries. | Full unit: 3309 passed, 19 baseline failures, 25 skipped. Corrected concurrent groups: 1234 passed plus 19 skips across attention, GDN, scheduler/memory, stage/metrics, MoE/parallel, timer/measurement, SGLang, and non-dummy/golden lanes. |
| Hardware boundary | `/root` | `/data/ycfeng/tmp/pr33-r10-nvidia-smi-L-b8cecf53.txt`, Python stack inventory | No device rows are visible on the CPU master. | NVIDIA: `SKIP: no visible NVIDIA device`; AMD/MI355X/ROCm/AITER/RCCL/HIP graph: `SKIP: AMD/MI355X hardware unavailable`. No runtime PASS inferred. |

Review status before the independent audit is retained above as historical evidence. Current authoritative status: **v1.1 is substantially incomplete**. No R item is fully complete; R01–R05, R07–R12, and R14 are PARTIAL, while R06 and R13 are NOT DONE. The 69-test focused CPU command passed, but real MLA routing, full GDN lifecycle, unified operator ownership, non-dummy/golden lanes, GPU entry points, performance causal closure, and R13 cleanup evidence remain open. D02 is a genuine pending decision; D03 remains conditional until the final cancellation inventory and lifecycle E2E are complete.

See `completion_audit_2026-09-16.md` and `test_report_2026-09-16_completion_audit.md`. Original review reports remain unchanged.
