## Modification History

| Date | Summary of Changes |
| --- | --- |
| 2026-09-16 | Added W08 owner verification closing the two observed trace-export failures. |
| 2026-09-16 | Reviewed 103 original attention/config/model-memory/training hunks and their current callers; added W01/W02/W09 local-remediation disposition and direct CPU evidence. |

# W11 attention, configuration, model-memory and training hunk review

Reviewer: `/root/w09_artifact_review`. Baseline: `0515589ac7f49ac5288a5f55b0ce38b0ede29bb2`. Original candidate/hunk IDs: `7b59d1fd7ad41f1301625f0b7c54578b5762f486`, as recorded in `changed_hunk_review.md`. Review read the current worktree (HEAD observed as `c1e924db2e337653f9a8f914948cee1f4e1d6350` plus concurrent uncommitted remediation). Original H IDs remain identifiers of original additions even where W01/W02/W09 replaced or removed their implementation.

This lane reviews **H001–H058, H229–H246, H437–H450, H456–H465 and H469–H471: 103 original hunks**. Predictor/runtime/profiling-directory hunks are covered by other reviewers; their callers are inspected here only to establish the boundary. `attention/profiling_mapping.py` belongs to this lane. No production code was changed during this review. W09 trainer/schema/persistence code was previously implemented by this same agent; this review does not misrepresent that portion as an independent second-author audit.

## Findings and evidence limits

The assigned hunks have a concrete retained, refactored or corrected implementation and caller. No original H ID in this scope remains unreviewed. The strongest new combined execution evidence is **223 passed in 14.91 s**; real multirow fitting, fresh artifact loading, invalid schema/identity rejection, interruption fault injection, automatic capacity arithmetic and existing dense/MLA behavior are exercised. This is CPU contract evidence, not measured GPU latency or native GDN numerical parity.

The zero-KV scheduler premise was investigated through normal model construction. Both explicit all-linear and interval-derived all-GDN schedules are rejected before scheduler construction by the supported Qwen3.5 hybrid structural requirement. No new all-GDN support is warranted by that path, and no zero-budget scheduler workaround was added. Two negative constructor regressions preserve this boundary.

Two production-constructor CPU E2Es initially passed admission/continuation/release assertions and failed trace expansion after the W08 EP integration. The W08 owner corrected expansion ownership and verified `test_stage_reporting_contract.py` plus `test_gdn_hybrid_e2e_increment14ab.py`: **18 passed in 12.06 s**, log `/data/ycfeng/tmp/pr33-w08-reporting-hybrid-2.log`, including all three originally blocked hybrid nodes. This closes the observed export failures. The W02 follow-up report preserves the initial failures, exact successful command, and attribution to the W08 owner. These synthetic CPU tests establish lifecycle/export contracts, not native parity.

Native boundary: **SKIP: AMD/MI355X hardware unavailable**. SKU throughput/memory/topology entries are simulator metadata. No ROCm discovery, AITER/MXFP4 kernel, recurrent-state tensor layout, native timing provenance or GPU truth was independently executed by this lane. Accepted D57 MLP/MoE/MTP two-byte parameter approximation remains explicit; arithmetic verification does not establish actual MXFP4 residency.

## Evidence index

All E01–E08 test files below were executed in the combined CPU command. These are checked assertions, not a claim that every branch of every listed file was reached.

| Evidence | Direct tests and scope |
| --- | --- |
| E01 | `test_gdn_semantic_core.py`, `test_attention_family_binding.py`: actual 92-layer Quark config resolves 69 GDN/23 full-attention layers; eight-layer fixture is 6/2 with dense IDs 3/7; explicit family binding; unrelated linear metadata remains non-GDN; CPU-safe imports. |
| E02 | `test_gdn_scheduler_slots.py`, `test_gdn_runtime_guards.py`, `test_gdn_state_lifecycle.py`: automatic planner/phase-aware capacity, independent fixed-state reservation arithmetic, nonincreasing floor result, structured OOM, non-GDN behavior, admission/continuation/release, failure rollback, preemption rejection, all-GDN structural rejection. |
| E03 | `test_gdn_increment6_memory.py`: TP-partitioned convolution/recurrent state, sharded GDN parameter count, FP32 A_log correction, actual stage layer counts, fixed reservation and full-attention-only KV denominator. Its PP2 helper arithmetic is not supported PP2 GDN Simulator evidence. |
| E04 | `test_attention_family_specs.py`, `test_model_architecture_registry.py`, `test_quantization_manager_registry.py`: profile APIs and migrated projection terminology, typed family registry, trainer profile reuse, cached runtime identity/serialization, inactive GDN metadata exclusion and null selector hash compatibility. |
| E05 | `test_gdn_training_predictor_increment8.py`: real six-task training/load, measurement/TP identity separation, structured timings, estimator extrapolation without clipping, public standard input path and reuse without fitting. |
| E06 | `test_gdn_artifact_boundary.py`: actual multirow fit and fresh load, exact and estimator branches, swapped/duplicate/missing task rejection, metadata/features/target/identity mismatches, null/mixed selected scopes, finite nonnegative predictions, one-row policy and cache reuse, interrupted pickle/JSON publication. |
| E07 | `test_param_counter_typed_layer_contract.py`, `test_param_counter_share_expert_memory_contract.py`: existing typed dense/routed layer and shared-expert parameter-memory behavior. |
| E08 | `test_profiling_accelerator.py::test_mi355x_device_and_ubb_node_configs_are_registered`: CPU registry/factory resolution of declared MI355X and eight-device UBB metadata; hardware performance is not tested. |

## Original hunk dispositions

Each original H ID has its own row so the main hunk ledger can import dispositions without inferring coverage from a file-level label.

| Original H ID | File | Disposition | Current owner, caller and rationale | Evidence |
| --- | --- | --- | --- | --- |
| H001 | `frontier/attention/__init__.py` | RETAIN | Package exports point to the existing family registry, layer binding and runtime-layout resolver; no second semantic family identity. Imports exercised by normal BaseModelConfig and predictor construction. | E01, E04 |
| H002 | `frontier/attention/__init__.py` | RETAIN | Package exports point to the existing family registry, layer binding and runtime-layout resolver; no second semantic family identity. Imports exercised by normal BaseModelConfig and predictor construction. | E01, E04 |
| H003 | `frontier/attention/__init__.py` | RETAIN | Package exports point to the existing family registry, layer binding and runtime-layout resolver; no second semantic family identity. Imports exercised by normal BaseModelConfig and predictor construction. | E01, E04 |
| H004 | `frontier/attention/__init__.py` | RETAIN | Package exports point to the existing family registry, layer binding and runtime-layout resolver; no second semantic family identity. Imports exercised by normal BaseModelConfig and predictor construction. | E01, E04 |
| H005 | `frontier/attention/__init__.py` | RETAIN | Package exports point to the existing family registry, layer binding and runtime-layout resolver; no second semantic family identity. Imports exercised by normal BaseModelConfig and predictor construction. | E01, E04 |
| H006 | `frontier/attention/__init__.py` | RETAIN | Package exports point to the existing family registry, layer binding and runtime-layout resolver; no second semantic family identity. Imports exercised by normal BaseModelConfig and predictor construction. | E01, E04 |
| H007 | `frontier/attention/__init__.py` | RETAIN | Package exports point to the existing family registry, layer binding and runtime-layout resolver; no second semantic family identity. Imports exercised by normal BaseModelConfig and predictor construction. | E01, E04 |
| H008 | `frontier/attention/families.py` | RETAIN | GATED_DELTA_NET_ATTENTION_FAMILY registers four physical operations, prefill/decode phases and FIXED_STATE layout; GDN_TASKS derives six phase-qualified tasks from this declaration. MIXED is not admitted. | E01, E05 |
| H009 | `frontier/attention/families.py` | RETAIN | GATED_DELTA_NET_ATTENTION_FAMILY registers four physical operations, prefill/decode phases and FIXED_STATE layout; GDN_TASKS derives six phase-qualified tasks from this declaration. MIXED is not admitted. | E01, E05 |
| H010 | `frontier/attention/families.py` | RETAIN | GATED_DELTA_NET_ATTENTION_FAMILY registers four physical operations, prefill/decode phases and FIXED_STATE layout; GDN_TASKS derives six phase-qualified tasks from this declaration. MIXED is not admitted. | E01, E05 |
| H011 | `frontier/attention/gdn/__init__.py` | RETAIN | gdn package exports CPU-only shape, state, guard and feature contracts; runtime kernels stay outside this package. | E01 CPU-safe import |
| H012 | `frontier/attention/gdn/config.py` | BUGFIX | Original parallel layer resolver removed. GatedDeltaNetConfig owns dimensions/state layout and exact Qwen profile activation; LayerAttentionSpec.is_full_attention resolves registry memory layout. model_binding.resolve_attention_topology is the sole topology normalization owner. | E01, E02; W01 source migration |
| H013 | `frontier/attention/gdn/features.py` | BUGFIX | GDNBatchFeatures remains shared by trainer and runtime. Added GDN_TASKS, artifact schema version, complete identity columns and validation here, avoiding a runtime-to-training import. Decode feature vector excludes history length. | E05, E06 |
| H014 | `frontier/attention/gdn/guards.py` | RETAIN | validate_gdn_runtime_support is called from ReplicaConfig, SimulationConfig, scheduler initialization/preemption and KV-transfer boundary. Unsupported prefix, PD, speculative, PP/EP/DP and cross-node state semantics remain fail-fast. | E02 guards; supported lifecycle only |
| H015 | `frontier/attention/gdn/memory.py` | RETAIN | Fixed per-request layout accounts convolution history in 2-byte elements and recurrent state in 4-byte elements; MemoryPlanner multiplies total_bytes by resident GDN layers and admitted capacity. No tensor allocation or native layout execution is claimed. | E03 arithmetic |
| H016 | `frontier/attention/gdn/state.py` | RETAIN | Request-keyed slot tokens retain/resume identity, reject duplicate/exhausted allocation and release idempotently. Scheduler admission commits KV plus owner atomically, release-by-ID covers owners absent from queues. No request-cancel API was added. | E02 lifecycle; W02 report |
| H017 | `frontier/attention/model_binding.py` | REFACTOR | model_binding imports normalized GDN shape/schedule helpers; duplicate family derivation moved to resolve_attention_topology. | E01 |
| H018 | `frontier/attention/model_binding.py` | RETAIN | Whole-model bind_attention_family rejects hybrid execution without explicit layer identity; homogeneous dense/MLA/MFA behavior retains the established binder. | E01 explicit layer and old family regressions |
| H019 | `frontier/attention/model_binding.py` | BUGFIX | Per-layer binding reads model-owned immutable LayerAttentionSpec; homogeneous topology uses the same established family binder, repairing dense-head misclassification of MLA. Runtime KV metadata resolves the unique full-attention family. | E01; source BaseModelConfig.get_layer_attention_specs |
| H020 | `frontier/attention/ops.py` | RETAIN | FIXED_STATE is an enum member consumed by registered GDN family and feature routing, separate from growing dense KV or latent MLA memory. | E01, E05 |
| H021 | `frontier/attention/profiling_mapping.py` | REFACTOR | Both ordinary and shared feature-map APIs reference GDN_FEATURE_COLUMNS instead of separate five-column tuples; existing predictor-name coverage guard still checks the family registry. | E04, E05 |
| H022 | `frontier/attention/profiling_mapping.py` | REFACTOR | Both ordinary and shared feature-map APIs reference GDN_FEATURE_COLUMNS instead of separate five-column tuples; existing predictor-name coverage guard still checks the family registry. | E04, E05 |
| H023 | `frontier/config/config.py` | RETAIN | ReplicaConfig calls shared GDN guard after resolved model/topology construction, rejecting unsupported speculation, PP/EP/attention-DP and cross-node execution. | E02, normal constructor path |
| H024 | `frontier/config/config.py` | RETAIN | Public gdn_input_file uses standard compute taxonomy and is consumed by resolved measurement paths and shared prediction-model loading. Experimental kernel-only paths are not public config aliases. | E05 public-path assertion; path implementation reviewed by other lane |
| H025 | `frontier/config/config.py` | RETAIN | SimulationConfig invokes a shared guard after cluster replicas exist, including MONOLITHIC and role-specific replicas; prefix/PD enablement is checked before simulation. | E02 guards; config source/call order |
| H026 | `frontier/config/config.py` | RETAIN | SimulationConfig invokes a shared guard after cluster replicas exist, including MONOLITHIC and role-specific replicas; prefix/PD enablement is checked before simulation. | E02 guards; config source/call order |
| H027 | `frontier/config/device_sku_config.py` | RETAIN | Base GPU platform metadata and MI355X SKU are resolved through existing fixed-config registration; 2516 TFLOPS and 288 GiB are declared config inputs, not measured device facts. | E08 registration only; native verification SKIP |
| H028 | `frontier/config/device_sku_config.py` | RETAIN | Base GPU platform metadata and MI355X SKU are resolved through existing fixed-config registration; 2516 TFLOPS and 288 GiB are declared config inputs, not measured device facts. | E08 registration only; native verification SKIP |
| H029 | `frontier/config/model_config.py` | REFACTOR | BaseModelConfig imports canonical resolve_attention_topology/runtime-family APIs and CPU structural types. Model get_gdn_config/get_layer_attention_specs share one cached normalized result. | E01, E04 |
| H030 | `frontier/config/model_config.py` | RETAIN | QuantizationConfig accepts fp4 and selected quantized_operations, includes selection in stable signatures, normalizes supported Quark weight/input fp4 metadata, preserves ignored/excluded layers and round-trips fields. QuantizationManager checks selected op names; this is config translation, not MXFP4 storage or kernel validation. | E01 real Quark config construction, E04 metadata hash regression; source from_dict/to_dict |
| H031 | `frontier/config/model_config.py` | RETAIN | QuantizationConfig accepts fp4 and selected quantized_operations, includes selection in stable signatures, normalizes supported Quark weight/input fp4 metadata, preserves ignored/excluded layers and round-trips fields. QuantizationManager checks selected op names; this is config translation, not MXFP4 storage or kernel validation. | E01 real Quark config construction, E04 metadata hash regression; source from_dict/to_dict |
| H032 | `frontier/config/model_config.py` | RETAIN | QuantizationConfig accepts fp4 and selected quantized_operations, includes selection in stable signatures, normalizes supported Quark weight/input fp4 metadata, preserves ignored/excluded layers and round-trips fields. QuantizationManager checks selected op names; this is config translation, not MXFP4 storage or kernel validation. | E01 real Quark config construction, E04 metadata hash regression; source from_dict/to_dict |
| H033 | `frontier/config/model_config.py` | RETAIN | QuantizationConfig accepts fp4 and selected quantized_operations, includes selection in stable signatures, normalizes supported Quark weight/input fp4 metadata, preserves ignored/excluded layers and round-trips fields. QuantizationManager checks selected op names; this is config translation, not MXFP4 storage or kernel validation. | E01 real Quark config construction, E04 metadata hash regression; source from_dict/to_dict |
| H034 | `frontier/config/model_config.py` | RETAIN | QuantizationConfig accepts fp4 and selected quantized_operations, includes selection in stable signatures, normalizes supported Quark weight/input fp4 metadata, preserves ignored/excluded layers and round-trips fields. QuantizationManager checks selected op names; this is config translation, not MXFP4 storage or kernel validation. | E01 real Quark config construction, E04 metadata hash regression; source from_dict/to_dict |
| H035 | `frontier/config/model_config.py` | RETAIN | QuantizationConfig accepts fp4 and selected quantized_operations, includes selection in stable signatures, normalizes supported Quark weight/input fp4 metadata, preserves ignored/excluded layers and round-trips fields. QuantizationManager checks selected op names; this is config translation, not MXFP4 storage or kernel validation. | E01 real Quark config construction, E04 metadata hash regression; source from_dict/to_dict |
| H036 | `frontier/config/model_config.py` | RETAIN | QuantizationConfig accepts fp4 and selected quantized_operations, includes selection in stable signatures, normalizes supported Quark weight/input fp4 metadata, preserves ignored/excluded layers and round-trips fields. QuantizationManager checks selected op names; this is config translation, not MXFP4 storage or kernel validation. | E01 real Quark config construction, E04 metadata hash regression; source from_dict/to_dict |
| H037 | `frontier/config/model_config.py` | RETAIN | QuantizationConfig accepts fp4 and selected quantized_operations, includes selection in stable signatures, normalizes supported Quark weight/input fp4 metadata, preserves ignored/excluded layers and round-trips fields. QuantizationManager checks selected op names; this is config translation, not MXFP4 storage or kernel validation. | E01 real Quark config construction, E04 metadata hash regression; source from_dict/to_dict |
| H038 | `frontier/config/model_config.py` | RETAIN | QuantizationConfig accepts fp4 and selected quantized_operations, includes selection in stable signatures, normalizes supported Quark weight/input fp4 metadata, preserves ignored/excluded layers and round-trips fields. QuantizationManager checks selected op names; this is config translation, not MXFP4 storage or kernel validation. | E01 real Quark config construction, E04 metadata hash regression; source from_dict/to_dict |
| H039 | `frontier/config/model_config.py` | RETAIN | Model schema carries architecture aliases and explicit hybrid schedule/dimensions, interpreted only for exact registered Qwen3.5 profile. Generic/Qwen3-next linear metadata does not activate GDN. | E01 negative profile activation |
| H040 | `frontier/config/model_config.py` | RETAIN | Model schema carries architecture aliases and explicit hybrid schedule/dimensions, interpreted only for exact registered Qwen3.5 profile. Generic/Qwen3-next linear metadata does not activate GDN. | E01 negative profile activation |
| H041 | `frontier/config/model_config.py` | BUGFIX | Model-owned topology cache stores immutable per-layer identities and normalized GDN shape together; lazy resolution preserves legacy validation order and avoids duplicate shape/schedule normalization. Bound-checks reject invalid global layer IDs. | E01; W01 source migration |
| H042 | `frontier/config/model_config.py` | BUGFIX | Model-owned topology cache stores immutable per-layer identities and normalized GDN shape together; lazy resolution preserves legacy validation order and avoids duplicate shape/schedule normalization. Bound-checks reject invalid global layer IDs. | E01; W01 source migration |
| H043 | `frontier/config/model_config.py` | BUGFIX | Model-owned topology cache stores immutable per-layer identities and normalized GDN shape together; lazy resolution preserves legacy validation order and avoids duplicate shape/schedule normalization. Bound-checks reject invalid global layer IDs. | E01; W01 source migration |
| H044 | `frontier/config/model_config.py` | RETAIN | JSON loading validates partial_rotary_factor, preserves architecture aliases, normalizes layer_types and optional GDN dimensions, and passes them to the normal model constructor. Physical layout follows the configured schedule rather than a model-name heuristic. | E01 real JSON 92-layer config and eight-layer fixture; parser source inspection |
| H045 | `frontier/config/model_config.py` | RETAIN | JSON loading validates partial_rotary_factor, preserves architecture aliases, normalizes layer_types and optional GDN dimensions, and passes them to the normal model constructor. Physical layout follows the configured schedule rather than a model-name heuristic. | E01 real JSON 92-layer config and eight-layer fixture; parser source inspection |
| H046 | `frontier/config/model_config.py` | RETAIN | JSON loading validates partial_rotary_factor, preserves architecture aliases, normalizes layer_types and optional GDN dimensions, and passes them to the normal model constructor. Physical layout follows the configured schedule rather than a model-name heuristic. | E01 real JSON 92-layer config and eight-layer fixture; parser source inspection |
| H047 | `frontier/config/model_config.py` | RETAIN | JSON loading validates partial_rotary_factor, preserves architecture aliases, normalizes layer_types and optional GDN dimensions, and passes them to the normal model constructor. Physical layout follows the configured schedule rather than a model-name heuristic. | E01 real JSON 92-layer config and eight-layer fixture; parser source inspection |
| H048 | `frontier/config/model_config.py` | RETAIN | JSON loading validates partial_rotary_factor, preserves architecture aliases, normalizes layer_types and optional GDN dimensions, and passes them to the normal model constructor. Physical layout follows the configured schedule rather than a model-name heuristic. | E01 real JSON 92-layer config and eight-layer fixture; parser source inspection |
| H049 | `frontier/config/node_sku_config.py` | RETAIN | MI355X_UBB declares eight physical devices independent of attention TP width, using existing node/device enums and factories. | E08 registration; native topology SKIP |
| H050 | `frontier/config/quantization_manager.py` | RETAIN | QuantizationManager tracks active GDN family for emitted metadata, validates per-operation quant selectors, and omits a null optional selector from persisted config identity. Dense metadata/hash fidelity and hybrid metadata presence are explicitly tested. | E04 quantization registry and config hash tests |
| H051 | `frontier/config/quantization_manager.py` | RETAIN | QuantizationManager tracks active GDN family for emitted metadata, validates per-operation quant selectors, and omits a null optional selector from persisted config identity. Dense metadata/hash fidelity and hybrid metadata presence are explicitly tested. | E04 quantization registry and config hash tests |
| H052 | `frontier/config/quantization_manager.py` | RETAIN | QuantizationManager tracks active GDN family for emitted metadata, validates per-operation quant selectors, and omits a null optional selector from persisted config identity. Dense metadata/hash fidelity and hybrid metadata presence are explicitly tested. | E04 quantization registry and config hash tests |
| H053 | `frontier/config/quantization_manager.py` | RETAIN | QuantizationManager tracks active GDN family for emitted metadata, validates per-operation quant selectors, and omits a null optional selector from persisted config identity. Dense metadata/hash fidelity and hybrid metadata presence are explicitly tested. | E04 quantization registry and config hash tests |
| H054 | `frontier/config/quantization_manager.py` | RETAIN | QuantizationManager tracks active GDN family for emitted metadata, validates per-operation quant selectors, and omits a null optional selector from persisted config identity. Dense metadata/hash fidelity and hybrid metadata presence are explicitly tested. | E04 quantization registry and config hash tests |
| H055 | `frontier/config/quantization_manager.py` | RETAIN | QuantizationManager tracks active GDN family for emitted metadata, validates per-operation quant selectors, and omits a null optional selector from persisted config identity. Dense metadata/hash fidelity and hybrid metadata presence are explicitly tested. | E04 quantization registry and config hash tests |
| H056 | `frontier/config/quantization_manager.py` | RETAIN | QuantizationManager tracks active GDN family for emitted metadata, validates per-operation quant selectors, and omits a null optional selector from persisted config identity. Dense metadata/hash fidelity and hybrid metadata presence are explicitly tested. | E04 quantization registry and config hash tests |
| H057 | `frontier/config/quantization_manager.py` | RETAIN | QuantizationManager tracks active GDN family for emitted metadata, validates per-operation quant selectors, and omits a null optional selector from persisted config identity. Dense metadata/hash fidelity and hybrid metadata presence are explicitly tested. | E04 quantization registry and config hash tests |
| H058 | `frontier/config/utils.py` | RETAIN | dataclass_to_dict excludes private caches before recursion, matching existing private __dict__ exclusion. Persisted configs contain public inputs rather than lazy topology snapshots. | E04 runtime model-cache serialization tests; source serializer |
| H229 | `frontier/model_architectures.py` | REFACTOR | Renamed linear projection profiling types/field to AttentionLinearOpImplementation, AttentionLinearOpProfile and attention_linear_ops, distinguishing projections from recurrent GDN attention. AttentionTrainer and profiling/predictor callers migrate through the same profile field. | E04 architecture registry/AttentionTrainer tests; caller search |
| H230 | `frontier/model_architectures.py` | REFACTOR | Renamed linear projection profiling types/field to AttentionLinearOpImplementation, AttentionLinearOpProfile and attention_linear_ops, distinguishing projections from recurrent GDN attention. AttentionTrainer and profiling/predictor callers migrate through the same profile field. | E04 architecture registry/AttentionTrainer tests; caller search |
| H231 | `frontier/model_architectures.py` | REFACTOR | Renamed linear projection profiling types/field to AttentionLinearOpImplementation, AttentionLinearOpProfile and attention_linear_ops, distinguishing projections from recurrent GDN attention. AttentionTrainer and profiling/predictor callers migrate through the same profile field. | E04 architecture registry/AttentionTrainer tests; caller search |
| H232 | `frontier/model_architectures.py` | REFACTOR | Renamed linear projection profiling types/field to AttentionLinearOpImplementation, AttentionLinearOpProfile and attention_linear_ops, distinguishing projections from recurrent GDN attention. AttentionTrainer and profiling/predictor callers migrate through the same profile field. | E04 architecture registry/AttentionTrainer tests; caller search |
| H233 | `frontier/model_architectures.py` | RETAIN | Architecture profiles explicitly declare experimental status and nonempty runtime limitations; declarations do not grant unsupported execution. | E01 real profile and E02 guard tests |
| H234 | `frontier/model_architectures.py` | REFACTOR | Renamed linear projection profiling types/field to AttentionLinearOpImplementation, AttentionLinearOpProfile and attention_linear_ops, distinguishing projections from recurrent GDN attention. AttentionTrainer and profiling/predictor callers migrate through the same profile field. | E04 architecture registry/AttentionTrainer tests; caller search |
| H235 | `frontier/model_architectures.py` | REFACTOR | Renamed linear projection profiling types/field to AttentionLinearOpImplementation, AttentionLinearOpProfile and attention_linear_ops, distinguishing projections from recurrent GDN attention. AttentionTrainer and profiling/predictor callers migrate through the same profile field. | E04 architecture registry/AttentionTrainer tests; caller search |
| H236 | `frontier/model_architectures.py` | RETAIN | Architecture profiles explicitly declare experimental status and nonempty runtime limitations; declarations do not grant unsupported execution. | E01 real profile and E02 guard tests |
| H237 | `frontier/model_architectures.py` | REFACTOR | Renamed linear projection profiling types/field to AttentionLinearOpImplementation, AttentionLinearOpProfile and attention_linear_ops, distinguishing projections from recurrent GDN attention. AttentionTrainer and profiling/predictor callers migrate through the same profile field. | E04 architecture registry/AttentionTrainer tests; caller search |
| H238 | `frontier/model_architectures.py` | REFACTOR | Renamed linear projection profiling types/field to AttentionLinearOpImplementation, AttentionLinearOpProfile and attention_linear_ops, distinguishing projections from recurrent GDN attention. AttentionTrainer and profiling/predictor callers migrate through the same profile field. | E04 architecture registry/AttentionTrainer tests; caller search |
| H239 | `frontier/model_architectures.py` | REFACTOR | Renamed linear projection profiling types/field to AttentionLinearOpImplementation, AttentionLinearOpProfile and attention_linear_ops, distinguishing projections from recurrent GDN attention. AttentionTrainer and profiling/predictor callers migrate through the same profile field. | E04 architecture registry/AttentionTrainer tests; caller search |
| H240 | `frontier/model_architectures.py` | RETAIN | Qwen3.5 MoE is one registry entry with exact model/architecture identity and complete hybrid structural requirements. Requires both GDN and full-attention layers; all-GDN positive scheduler premise is rejected at normal construction. | E01, E02 new zero-KV-layout rejection tests |
| H241 | `frontier/model_architectures.py` | RETAIN | Qwen3.5 MoE is one registry entry with exact model/architecture identity and complete hybrid structural requirements. Requires both GDN and full-attention layers; all-GDN positive scheduler premise is rejected at normal construction. | E01, E02 new zero-KV-layout rejection tests |
| H242 | `frontier/model_architectures.py` | RETAIN | Registry resolves explicit or matched profiles and announces experimental limitations once per profile. Explicit invalid identities still fail instead of silently falling back to generic. | E04 architecture registry negative/resolution tests |
| H243 | `frontier/model_architectures.py` | RETAIN | Registry resolves explicit or matched profiles and announces experimental limitations once per profile. Explicit invalid identities still fail instead of silently falling back to generic. | E04 architecture registry negative/resolution tests |
| H244 | `frontier/model_architectures.py` | RETAIN | Registry resolves explicit or matched profiles and announces experimental limitations once per profile. Explicit invalid identities still fail instead of silently falling back to generic. | E04 architecture registry negative/resolution tests |
| H245 | `frontier/model_architectures.py` | RETAIN | Registry resolves explicit or matched profiles and announces experimental limitations once per profile. Explicit invalid identities still fail instead of silently falling back to generic. | E04 architecture registry negative/resolution tests |
| H246 | `frontier/model_architectures.py` | RETAIN | Registers Qwen profile once before generic fallback; adding a future architecture remains a profile registration rather than caller-name branching. | E01, E04 |
| H437 | `frontier/scheduler/utils/memory_planner.py` | RETAIN | MemoryPlanner imports family and per-layer binding for the full-attention KV layout, retaining ordinary binder for homogeneous models. | E03 |
| H438 | `frontier/scheduler/utils/memory_planner.py` | RETAIN | max_num_seqs is supplied from BaseReplicaScheduler effective admitted capacity and validated positive; missing capacity cannot silently omit nonzero GDN reservation. | E02 automatic capacity, E03 |
| H439 | `frontier/scheduler/utils/memory_planner.py` | RETAIN | max_num_seqs is supplied from BaseReplicaScheduler effective admitted capacity and validated positive; missing capacity cannot silently omit nonzero GDN reservation. | E02 automatic capacity, E03 |
| H440 | `frontier/scheduler/utils/memory_planner.py` | RETAIN | Hybrid KV element size comes from a real full-attention layer; GDN fixed state is accounted separately. The no-full-layer helper path does not imply supported all-GDN construction. | E03, E02 model guard boundary |
| H441 | `frontier/scheduler/utils/memory_planner.py` | REFACTOR | MemoryPlanner now asks ParamCounter.get_parameter_memory_per_device_bytes rather than multiplying total parameter elements uniformly by two. FP32 A_log bytes are preserved. | E03 parameter byte oracle |
| H442 | `frontier/scheduler/utils/memory_planner.py` | REFACTOR | Per-request KV uses resident full-attention layer count; GDN bytes are fixed. Stage counts and resident stage selection use public ParamCounter accessors, removing private cross-module totals inspection. | E03; E02 real PP1 automatic planner |
| H443 | `frontier/scheduler/utils/memory_planner.py` | REFACTOR | Per-request KV uses resident full-attention layer count; GDN bytes are fixed. Stage counts and resident stage selection use public ParamCounter accessors, removing private cross-module totals inspection. | E03; E02 real PP1 automatic planner |
| H444 | `frontier/scheduler/utils/memory_planner.py` | RETAIN | Reservation is admitted capacity multiplied by per-request resident state bytes; the required effective capacity is shared with scheduler slot ownership. | E02 independent arithmetic, phase-aware cap matrix |
| H445 | `frontier/scheduler/utils/memory_planner.py` | RETAIN | Available budget subtracts weights, accepted overhead and GDN reservation before floor division by actual KV-carrying layers. OOM details include state reservation; capacity monotonicity is nonincreasing because integer floors may tie. | E02 real automatic planner/OOM/non-GDN checks, E03 arithmetic |
| H446 | `frontier/scheduler/utils/memory_planner.py` | RETAIN | Available budget subtracts weights, accepted overhead and GDN reservation before floor division by actual KV-carrying layers. OOM details include state reservation; capacity monotonicity is nonincreasing because integer floors may tie. | E02 real automatic planner/OOM/non-GDN checks, E03 arithmetic |
| H447 | `frontier/scheduler/utils/memory_planner.py` | RETAIN | Available budget subtracts weights, accepted overhead and GDN reservation before floor division by actual KV-carrying layers. OOM details include state reservation; capacity monotonicity is nonincreasing because integer floors may tie. | E02 real automatic planner/OOM/non-GDN checks, E03 arithmetic |
| H448 | `frontier/scheduler/utils/memory_planner.py` | RETAIN | Available budget subtracts weights, accepted overhead and GDN reservation before floor division by actual KV-carrying layers. OOM details include state reservation; capacity monotonicity is nonincreasing because integer floors may tie. | E02 real automatic planner/OOM/non-GDN checks, E03 arithmetic |
| H449 | `frontier/scheduler/utils/memory_planner.py` | RETAIN | Available budget subtracts weights, accepted overhead and GDN reservation before floor division by actual KV-carrying layers. OOM details include state reservation; capacity monotonicity is nonincreasing because integer floors may tie. | E02 real automatic planner/OOM/non-GDN checks, E03 arithmetic |
| H450 | `frontier/scheduler/utils/memory_planner.py` | RETAIN | Available budget subtracts weights, accepted overhead and GDN reservation before floor division by actual KV-carrying layers. OOM details include state reservation; capacity monotonicity is nonincreasing because integer floors may tie. | E02 real automatic planner/OOM/non-GDN checks, E03 arithmetic |
| H456 | `frontier/training/__init__.py` | RETAIN | GDNTrainer uses existing training package export; runtime predictor does not import this module to acquire task schema. | E05 import/train/load |
| H457 | `frontier/training/__init__.py` | RETAIN | GDNTrainer uses existing training package export; runtime predictor does not import this module to acquire task schema. | E05 import/train/load |
| H458 | `frontier/training/__init__.py` | RETAIN | Removal of final blank line in training package export list has no behavioral effect; no separate validation claim. | Source diff inspection |
| H459 | `frontier/training/attention_trainer.py` | REFACTOR | AttentionTrainer required-column discovery migrated to architecture_profile.attention_linear_ops; no independent projection-op list added. | E04 AttentionTrainer profile reuse |
| H460 | `frontier/training/base_trainer.py` | RETAIN | BaseTrainer cache hash inputs include standard gdn_input_file consistently with shared predictor artifact identity. Existing generic CV/cache algorithm remains in BaseTrainer. | E05 cache load/reuse, E06 fit cache reuse |
| H461 | `frontier/training/cli.py` | RETAIN | CLI registers gdn subcommand with explicit measurement family, identity selectors and estimator parameters, invokes GDNTrainer, reports training failure and dispatches from main. Defaults contain exactly one RF candidate, so supported one-row synthetic fixtures have a defined fit policy. | E05 actual trainer, prior W09 CLI/E2E evidence; CLI source dispatch |
| H462 | `frontier/training/cli.py` | RETAIN | CLI registers gdn subcommand with explicit measurement family, identity selectors and estimator parameters, invokes GDNTrainer, reports training failure and dispatches from main. Defaults contain exactly one RF candidate, so supported one-row synthetic fixtures have a defined fit policy. | E05 actual trainer, prior W09 CLI/E2E evidence; CLI source dispatch |
| H463 | `frontier/training/cli.py` | RETAIN | CLI registers gdn subcommand with explicit measurement family, identity selectors and estimator parameters, invokes GDNTrainer, reports training failure and dispatches from main. Defaults contain exactly one RF candidate, so supported one-row synthetic fixtures have a defined fit policy. | E05 actual trainer, prior W09 CLI/E2E evidence; CLI source dispatch |
| H464 | `frontier/training/cli.py` | RETAIN | CLI registers gdn subcommand with explicit measurement family, identity selectors and estimator parameters, invokes GDNTrainer, reports training failure and dispatches from main. Defaults contain exactly one RF candidate, so supported one-row synthetic fixtures have a defined fit policy. | E05 actual trainer, prior W09 CLI/E2E evidence; CLI source dispatch |
| H465 | `frontier/training/gdn_trainer.py` | BUGFIX | Trainer validates the complete selected identity including nulls; fits only canonical phase tasks and finite nonnegative targets; validates all tasks before publishing final files. Shared atomic pickle/JSON writer and CSV fingerprint remove local duplicates. One row requires exactly one ParameterGrid candidate; multiple candidates fail before arbitrary selection. | E05, E06 actual multirow fit/fresh load, corruption and interruption fault injection |
| H469 | `frontier/utils/param_counter.py` | REFACTOR | ParamCounter owns stage attention counts, resident selection, GDN sharded parameter elements and FP32 A_log bytes. MemoryPlanner uses public stage-count/selection APIs. PP arithmetic tests do not grant unsupported PP>1 GDN execution. | E03 arithmetic; E02 PP1 normal constructor |
| H470 | `frontier/utils/param_counter.py` | RETAIN | Per-device total combines actual attention/GDN stage elements with existing dense/MoE/MTP counts. Dense, routed and share-expert regressions protect homogeneous behavior. | E03, E07 |
| H471 | `frontier/utils/param_counter.py` | RETAIN | Public parameter-byte result uses GDN-specific bytes but preserves accepted D57 two-byte MLP/MoE/MTP approximation. No MXFP4 capacity scaling or OOM bypass was introduced. | E03, E07; explicit D57 source comment |

## Local remediation added after the original hunk snapshot

| Current local change | Original relationship | Disposition and caller evidence |
| --- | --- | --- |
| `resolve_attention_topology`, one cached shape/spec result, registered-family `is_full_attention` | Replaces duplicated classification inside H012/H019/H043 | BUGFIX/REFACTOR: homogeneous MLA uses established binder and GDN shape normalizes once; E01/E04. |
| `GDN_TASKS`, complete artifact identity/schema constants and normalizer in `attention/gdn/features.py` | Extends H013 and supersedes trainer-local task/schema literals in H465 | BUGFIX: trainer and GDN predictor import a dependency-light shared owner; E05/E06. |
| Selected-scope null/mixed validation and shared feature tuple in `attention/profiling_mapping.py` | Supplements H021/H022 | BUGFIX/REFACTOR: trainer validates after explicit selectors; E05/E06. |
| `ParamCounter.get_attention_stage_layer_counts` and `get_resident_attention_stage_id` | Refines H443/H469 | REFACTOR: MemoryPlanner no longer reads another module's private stage totals; E02/E03. |
| `cache_io.atomic_json_dump` and shared `dataset_fingerprint`, preserving `atomic_pickle_dump` interface | Artifact persistence supplement outside original H465 file | BUGFIX/REFACTOR at existing cache owner. Trainer imports helpers; final pickle and JSON are each atomically replaced. Fault injection preserves the prior destination. No multi-file snapshot transaction is claimed; model metadata rejects mixed identities. |
| GDN one-row policy and final publication after task validation | Replaces H465 arbitrary first-grid-item fitting/direct writes | BUGFIX: exactly one grid candidate directly fits/caches; multiple candidates require sufficient CV rows. Atomicity is per-file, not all-six-plus-manifest transactional publication; E06. |

## Size and responsibility decisions

`frontier/config/config.py` is the only assigned original changed module above 2,000 lines: pinned baseline **5,663**, original candidate **5,725**, current **5,725** lines (literal newline counts). Its added 62 lines retain dataclass fields and configuration-time guard wiring; substantive topology, shapes, lifecycle and training logic live in existing attention/GDN/training owners. This lane recommends retaining the bounded adapter changes and deferring any wholesale config-file split to a separately justified task. Such a split would touch CLI flattening and broad configuration contracts without removing the duplication actually corrected here. No source formatter churn was introduced.

Current supporting sizes: `model_config.py` 1,223, `model_architectures.py` 1,193, `training/base_trainer.py` 486, `training/cli.py` 722, `training/gdn_trainer.py` 301. W09 made the new trainer smaller than its original 341 lines while reusing task/features and persistence owners. Runtime critical-module LOC/split decisions are owned by the other W11 reviewers.

## Reproducible verification

Working directory: `/data/ycfeng/stepfun-performance-optimization/Frontier/.worktrees/feature-amd-sglang-gdn`. Interpreter `/usr/bin/python`, Python **3.12.3**; no conda activation command was used. Expected: all selected CPU assertions pass, including real fitted artifacts rather than fabricated fitted-object sentinels.

```bash
env PYTHONPATH="$PWD" PYTHONDONTWRITEBYTECODE=1 WANDB_DISABLED=true VIDUR_DISABLE_WANDB=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 TMPDIR=/data/ycfeng/tmp /usr/bin/python -m pytest tests/unit/test_gdn_semantic_core.py tests/unit/test_gdn_increment6_memory.py tests/unit/test_gdn_state_lifecycle.py tests/unit/test_gdn_runtime_guards.py tests/unit/test_gdn_scheduler_slots.py tests/unit/test_gdn_training_predictor_increment8.py tests/unit/test_gdn_artifact_boundary.py tests/unit/test_attention_family_specs.py tests/unit/test_attention_family_binding.py tests/unit/test_model_architecture_registry.py tests/unit/test_param_counter_typed_layer_contract.py tests/unit/test_param_counter_share_expert_memory_contract.py tests/unit/test_quantization_manager_registry.py tests/unit/test_profiling_accelerator.py::test_mi355x_device_and_ubb_node_configs_are_registered -q -p no:cacheprovider --basetemp /data/ycfeng/tmp/pr33-w11-attention-training > /data/ycfeng/tmp/pr33-w11-attention-training.log 2>&1
```

Observed: `223 passed in 14.91s`, exit code 0. Related actual E2E failures and lifecycle assertions are recorded in `test_report_2026-09-16_w02_slot_owner_final.md`; they are deliberately not counted as successful export validation here.
